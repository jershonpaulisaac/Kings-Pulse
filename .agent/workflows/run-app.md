---
description: How to run the Kings SkillNet Expo application
---

1. Open a terminal in the project directory.
2. Run `npm install` to ensure all dependencies are present.
3. Run `npm start` or `npx expo start` to launch the Expo development server.
4. Use the Expo Go app on your physical device to scan the QR code.
